# R3.2 多实例任务取消广播（Redis Pub/Sub）

> 阶段三 · 可靠性与运维 | 对应落地修复清单 #8：TaskRegistry 补 Redis Pub/Sub 取消广播，替代单机内存注册表
> 前置阅读：`docs/refactor/00_重构总览与执行指南.md`

## 1. 目标

实现**多实例部署下的取消一致性**：任一 API 实例收到 cancel 请求，所有 worker 实例上运行的对应任务都能终止。机制：本地未命中时通过 **Redis Pub/Sub 频道广播取消指令**，每个实例后台订阅该频道，收到与本地注册表匹配的 thread_id 即执行真正的 `task.cancel()`。

**验收标准**：

- [ ] `TaskRegistry.cancel(thread_id)` 本地命中 → 直接取消（现有行为不变）
- [ ] 本地未命中 → `PUBLISH task:cancel {thread_id}` 广播，其他实例订阅方取消本地任务
- [ ] 每个实例启动时建立订阅（后台 task），订阅断线自动重连
- [ ] 广播消息带时间戳与实例 ID（便于排查重复消息），消费端幂等（对已 done 任务无副作用）
- [ ] 无 Redis 时整体降级为纯本地取消（现有行为），不报错
- [ ] 单元测试全部通过，全量回归无破坏

## 2. 现状分析（动手前先完整阅读以下文件）

| 文件 | 现状要点 |
| --- | --- |
| `app/backend/service/task_registry.py` | `TaskRegistry`：`_tasks: dict[thread_id, RunningTask]`；`submit()` 注册 + done_callback 清理；`cancel(thread_id)`：本地命中 → `entry.task.cancel()` 返回 True；**本地未命中 → 向 Redis 写 `cancel:{thread_id} = "1"`（SET），但无任何订阅方消费该 key**；`scan_orphans()`（约 149 行）扫描 Redis 标记识别孤儿任务；`init_task_registry(redis=None)` 注入 Redis 连接；`get_task_registry()` 全局单例 |
| `app/backend/router/research_router.py` | `cancel_research` 路由调 `registry.cancel(payload.thread_id)`；`_stream_with_registry()` 包装 generator 注册/清理任务 |
| `app/backend/infra/` | Redis 客户端获取方式（执行时 grep `redis` 确认连接构建位置与是否为 redis.asyncio） |

**现状问题**：取消指令是「写一个 Redis key」但没人读——多实例部署时，任务在实例 A 运行、取消请求打到实例 B，B 的 `cancel()` 本地未命中只写了 key 并返回 False，A 上的任务继续跑完并持续消耗 LLM token。Redis SET 方案还随时间积累垃圾 key。

## 3. 详细设计

### 3.1 频道协议

```
频道名：task:cancel
消息体（JSON）：
{
  "thread_id": "abc123",
  "instance_id": "host-pid-uuid",        # 发起方实例标识（随机生成，进程级单例）
  "ts": 1725500000                       # Unix 秒级时间戳
}
```

消费端忽略 `instance_id` 等于自己的消息（本地未命中才广播，等价于自然过滤；保留该字段仅为日志排查）。

### 3.2 TaskRegistry 改造

```python
CANCEL_CHANNEL = "task:cancel"

class TaskRegistry:
    def __init__(self, ...):
        ...
        self._instance_id = f"{socket.gethostname()}-{os.getpid()}-{uuid.uuid4().hex[:6]}"
        self._subscriber_task: asyncio.Task | None = None

    async def cancel(self, thread_id: str) -> bool:
        entry = self._tasks.get(thread_id)
        if entry is not None and not entry.task.done():
            entry.task.cancel()
            return True
        # 本地未命中 → 广播（Redis 不可用时直接返回 False，不抛异常）
        if self._redis is not None:
            try:
                payload = json.dumps({
                    "thread_id": thread_id,
                    "instance_id": self._instance_id,
                    "ts": int(time.time()),
                })
                await self._redis.publish(CANCEL_CHANNEL, payload)
                # 写 running 标记的旧行为保留与否：读 scan_orphans 的语义后决定
                # （若 scan_orphans 依赖 cancel:{tid} 标记做孤儿判定，保留 SET 并加 TTL 300s 防垃圾堆积）
                await self._redis.setex(f"cancel:{thread_id}", 300, "1")
                return True      # 广播成功即认为已请求取消（无法同步确认远端）
            except Exception as exc:
                logger.warning("[task-registry] 取消广播失败 | thread=%s | %s", thread_id, exc)
                return False
        return False

    async def start_subscriber(self) -> None:
        """启动后台订阅（app lifespan 中调用一次）。"""
        if self._redis is None or self._subscriber_task is not None:
            return
        self._subscriber_task = asyncio.create_task(self._subscribe_loop())

    async def _subscribe_loop(self) -> None:
        """订阅循环：断线指数退避重连（1s 起步，上限 30s）。"""
        backoff = 1.0
        while True:
            try:
                pubsub = self._redis.pubsub()
                await pubsub.subscribe(CANCEL_CHANNEL)
                backoff = 1.0
                async for message in pubsub.listen():
                    if message.get("type") != "message":
                        continue
                    await self._handle_cancel_broadcast(message.get("data"))
            except asyncio.CancelledError:
                raise
            except Exception as exc:
                logger.warning("[task-registry] 订阅断开，%ss 后重连 | %s", backoff, exc)
                await asyncio.sleep(backoff)
                backoff = min(backoff * 2, 30.0)

    async def _handle_cancel_broadcast(self, raw) -> None:
        try:
            payload = json.loads(raw if isinstance(raw, str) else raw.decode())
        except Exception:
            return
        thread_id = payload.get("thread_id", "")
        entry = self._tasks.get(thread_id)
        if entry is not None and not entry.task.done():
            entry.task.cancel()
            logger.info("[task-registry] 远端取消命中本实例任务 | thread=%s | from=%s",
                        thread_id, payload.get("instance_id"))
```

实现注意：

- `self._redis` 必须是 `redis.asyncio` 客户端（执行时确认现有注入的客户端类型；若注入的是同步 redis，订阅需单独构建 async 客户端或用 `asyncio.to_thread` 包装同步 pubsub——以现有代码风格为准，优先 async 原生）
- `pubsub.listen()` 阻塞迭代在连接断开时抛异常 → 被 except 捕获重连
- 幂等：`entry.task.done()` 检查保证对已完成任务无副作用；重复广播消息无副作用

### 3.3 生命周期挂载

`app_main.py`（或 lifespan 所在文件，执行时确认 FastAPI 启动挂载点——现有 `init_task_registry` 的调用位置）：

```python
# 启动：应用 lifespan startup 中
await get_task_registry().start_subscriber()

# 关闭：shutdown 中
registry = get_task_registry()
if registry._subscriber_task is not None:
    registry._subscriber_task.cancel()      # 订阅循环随进程优雅退出
```

## 4. 实施步骤

### 步骤 1：读清 TaskRegistry 与 Redis 注入链

- 完整阅读 `task_registry.py`、`research_router.py` 的 cancel 路由、`app_main.py` 中 registry 初始化位置、Redis 客户端构建代码
- 产出：执行日志记录「Redis 客户端类型（sync/async）、scan_orphans 对 cancel:* key 的依赖、lifespan 挂载点」

### 步骤 2：实现广播与订阅

- 按 3.2 改造 `TaskRegistry`（cancel 广播 + 订阅循环 + 消息处理）
- 验证：`python -c "from backend.service.task_registry import TaskRegistry"` 无报错

### 步骤 3：生命周期挂载

- 按 3.3 在应用启动/关闭处挂载订阅
- 验证：`python -c "import app.app_main"` 无报错；启动服务日志出现订阅建立记录

### 步骤 4：编写测试

- 按 `TEST_阶段三测试用例.md` T3.2 组（Redis mock 为内存 dict 实现的 fake pubsub）
- 验证：`python -m pytest app/test/test_task_cancel_broadcast.py -q` 全绿

### 步骤 5：全量回归 + 双实例手动验证 + 提交

- `python -m pytest app/test -q` 全绿（重点关注 `test_p3.py` 现有取消语义用例）
- 有条件时双实例手动验证（见 TEST 文档 M3-01）
- 提交：`refactor(R3.2): 多实例任务取消广播`

## 5. 涉及文件清单

| 文件 | 操作 |
| --- | --- |
| `app/backend/service/task_registry.py` | 修改：cancel 广播、订阅循环、消息处理、instance_id |
| `app/app_main.py`（或 lifespan 所在文件） | 修改：订阅启动/停止挂载 |
| `app/test/test_task_cancel_broadcast.py` | 新增 |

## 6. 风险与回滚

- **风险 1**：`scan_orphans()` 依赖 `cancel:{tid}` key 的既有语义被破坏。缓解：步骤 1 先读语义；保留 SET 但加 TTL（300s）双保险，行为不变只防堆积。
- **风险 2**：订阅循环异常拖垮事件循环。缓解：订阅循环独立 task + 全局异常捕获 + 退避重连。
- **风险 3**：Redis 不可用时退化为现状（本地取消），语义与单机版一致，无新增故障面。
- **回滚**：广播逻辑只在本地未命中分支生效；revert 单 commit 后回到「写 key 无人读」的现状。
